## What this PR does

One-paragraph summary.

## Why

Context — what problem does this solve, what regression does this prevent.

## How tested

- [ ] `cargo fmt --all -- --check`
- [ ] `cargo clippy --all-targets --release -- -D warnings`
- [ ] `cargo test --release --bins`
- [ ] `bash tools/menubar/build.sh` (if Swift code touched)
- [ ] Manual smoke test: …

## Changelog

- Added entry to `CHANGELOG.md` under `[Unreleased]`
- Bumped `Cargo.toml` version: `vX.Y.Z` → `vX.Y.Z+1` (patch for fixes, minor for features)

## Screenshots

If a UI change.
