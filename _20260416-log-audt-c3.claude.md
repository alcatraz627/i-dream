# Quick Summary (for LLMs) — 2026-04-16T21:30:00+05:30

<!-- sessions: log-audt-c3@2026-04-16 -->

> This session was a log audit + bug investigation run. The user asked for a
> report on what is working, what isn't, and what needs fixing in the i-dream
> daemon. All 5 warning categories were triaged: 4 REM JSON parse failures were
> confirmed already fixed in commit `c517931`; the metacog batch truncation bug
> (malformed JSON sent to model on every cycle) was fixed in `c76745b`; ENOTCONN
> flood is expected graceful degradation, not a bug. The session is complete with
> one shipped fix and a clean diagnostic report. No blockers.

# Core Dump — 2026-04-16T21:30:00+05:30

## Initial Goal

Resumed from a compacted session. User's directive: "focus on checking the
runtime logs + outputs so far. Identify what is working, what is not working as
expected, the gaps, improvements, and other observations. Give me a report."
Pivoted mid-session to investigating the 4 REM JSON parse failures in detail and
fixing the metacog truncation bug discovered during the audit.

## Agent Actions

1. Resumed from pre-compact checkpoint — prior session had already run the log
   audit and identified 5 warning categories; REM investigation was in-progress
2. Read `src/api.rs:322-401` — `analyze_subprocess` passes `format_override`
   instructing Claude "raw JSON only — no fences"
3. Read `src/modules/dreaming.rs:125-156` — read full `parse_json_codeblock`
   implementation; logic looks correct for fenced and unfenced input
4. Read `dreaming.rs:594-626` — confirmed trace and parse both use identical
   `response.content` clone
5. Located 4 failing trace files from log timestamps in
   `~/.claude/subconscious/dreams/traces/`
6. Extracted raw bytes of `20260415-0511-a25fe1ff.jsonl` REM api_response
   payload — confirmed 0x60 0x60 0x60 (ASCII backticks), preamble text before
   the fence: `"Finding creative connections...\n\n```json\n[..."`
7. Python-simulated `parse_json_codeblock` on all 4 failing traces — returned
   FOUND for every one → current logic is correct
8. Ran `git log --oneline -- src/modules/dreaming.rs` — found commit `c517931`
   "Fix subprocess API mode, REM parsing" dated 2026-04-15T18:29
9. Read diff of `c517931` — confirmed root cause: OLD code required closing
   backtick fence via `if let Some(end) = after.find("```")` — no closing fence
   → returned `None`. Fixed to `unwrap_or(after.len())` + added `starts_with`
   guard. Failures are historical artifacts.
10. Checked ENOTCONN clustering — line 740 shows "Running manual dream cycle"
    immediately before the 08:21 cluster; daemon was not running as a service,
    this was a CLI manual invocation; ENOTCONN is expected graceful degradation
11. Scanned metacog truncation across all log files (04-12 through 04-16) —
    fires on EVERY cycle with ≥50 sessions, not just occasionally
12. Read `metacog.rs:473-490` — found char-boundary raw truncation leaves
    malformed JSON array (truncated mid-element) sent to model every cycle;
    WARN fired as noise
13. Fixed `src/modules/metacog.rs`: replaced char-boundary truncation with
    unit-boundary accumulation loop — collects complete `ExecutionUnit`s until
    `SAMPLE_BUDGET` (40k chars) is exhausted, stops cleanly; demoted WARN to
    INFO; added `units_total` alongside `units_analyzed` in audit JSON
14. Built with `cargo build` — clean (24 pre-existing unrelated warnings)
15. Ran `cargo test` — 263 pass (250 unit + 13 CLI integration)
16. Committed `c76745b` "Fix metacog batch truncation: use unit-boundary budget
    capping"
17. Delivered full 5-category status report with root causes and resolution
    status

## Current Expectation

The user requested a core dump to preserve this run for future reference. No
immediate next task specified. The diagnostic work is complete; the metacog fix
is shipped.

## Pending Items

- **ENOTCONN log noise**: 1,367 WARNs per 24h are expected behavior (hooks
  gracefully handle daemon absence) but they bury real errors in logs. Consider
  adding per-session dedup or exponential backoff to the hook scripts so they
  emit at most 1 WARN per daemon-down interval.
- **Metacog coverage**: With ~17/239 units analyzed per cycle (~7%), the
  analysis is heavily recency-biased toward the most recent 3-4 sessions. No
  bug, but raising the budget (e.g., to 80k) or adding random sampling could
  improve coverage without changing the architecture.
- **REM format_override compliance**: Claude at temperature 0.9 sometimes
  ignores the "no fences" instruction and adds preamble text. This is handled
  correctly by the current parser, but monitoring how often preamble appears
  could inform whether the format_override prompt needs strengthening.

## Session Insights

### What worked well

- `git log --oneline -- <file>` immediately surfaces fix commits — always check
  git history before deep-diving on a potential bug
- Python simulation of the Rust parser logic confirmed the current code is
  correct without needing to rebuild; useful short-circuit
- Reading raw bytes of the trace payload (`hex(b) for b in payload[:20].encode(...)`)
  eliminated any encoding/character-set hypothesis quickly
- Cross-checking truncation frequency across multiple log files (04-14 showed
  20+ occurrences) revealed the "6 times" in 24h from the session summary was
  an undercount

### What didn't work

- Initial assumption that `response.content` bytes in trace == what parser
  receives led to a longer search; the trace was correct all along — the bug was
  in the old version of the code, not the running version

### Gotchas encountered

- The 4 REM failures on 2026-04-15 predate the fix by about 13 hours — the fix
  shipped at 18:29 IST on 04-15, failures were at 05:13, 10:04, 12:13, 12:32.
  The WARN fires in the log even after the code is fixed because it's logged at
  runtime, not test time.
- `metacog.rs` old truncation cut at char boundary but left a JSON array
  truncated MID-ELEMENT — the model was receiving `[{...}, {...}, {"session_id":
"abc", "tool": "Read", "outcome":` (unfinished). Claude likely inferred the
  rest, but this is fundamentally broken.
- `units_analyzed` in the audit JSON stored `batch.units.len()` (total), not
  the budget-capped count — this made audit files misleading. Now stores both.
- ENOTCONN happens during manual CLI runs AND daemon downtime — they look
  identical in the log. Context clue: if "Running manual dream cycle" appears
  before the cluster, it's a CLI run (not a daemon crash).

### Notes for future agents

- `parse_json_codeblock` in `dreaming.rs:130-156` now handles: (a) fenced with
  closing fence, (b) fenced without closing fence (high-temp models often omit
  it), (c) preamble text before the fence, (d) bare triple-backtick fence, (e)
  raw JSON. No further changes needed unless Claude starts outputting something
  entirely new.
- `analyze_subprocess` in `api.rs:322` uses `format_override` via
  `--append-system-prompt` to suppress fences. It doesn't always work at
  temperature 0.9. The parser handles both cases correctly.
- Metacog analyzes the most-recent sessions first (due to `rev()` scan order in
  `load_new_samples`). With the 40k budget and typical session sizes, this
  covers ~3-4 recent sessions per cycle. This is intentional — recent activity
  is most useful for metacognitive calibration.
- ENOTCONN warnings are purely cosmetic — they don't affect Claude Code, don't
  lose data, and don't indicate a daemon crash. The daemon self-heals on next
  `i-dream start`.

---

_Generated by /core-dump. Resume with /catchup._
