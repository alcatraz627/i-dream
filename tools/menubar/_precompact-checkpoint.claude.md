# Pre-Compaction Checkpoint — 2026-04-17T06:01:30+05:30

<!-- sessions: 60f43456-8086-4373-8d52-8685ee80c71a@2026-04-17 -->

> Auto-generated by PreCompact hook (auto trigger).
> Shell-equivalent of /core-dump mini. Resume with: /catchup
> For full LLM-analyzed context, run: /core-dump

## Session Stats

| Field        | Value |
|--------------|-------|
| Trigger      | auto |
| Session ID   | 60f43456-8086-4373-8d52-8685ee80c71a |
| Branch       | master |
| Turns        | 55 |
| Tools used   | 2318 |
| Timestamp    | 2026-04-17T06:01:30+05:30 |

## User Goals

**From WAL:** Improve statusline script — MCP display, line splitting, priority layout, research docs
**From previous checkpoint:** _(no prior checkpoint goal found)_

> If these differ, the user likely pivoted mid-session. Resume the most recent goal.

## Working Directory

`/Users/alcatraz627/Code/Claude/i-dream/tools/menubar`

## Recovery Sequence

After compaction, run `/catchup` — it will:
1. Check WAL for the last CHECKPOINT block (fast path)
2. Fall back to this file for structured state
3. Load targeted file context from pending items
4. Ask which pending item to resume

---

## Recently Modified Files

- `.gitignore`
- `README.md`
- `src/config.rs`
- `src/daemon.rs`
- `src/modules/dreaming.rs`
- `src/modules/metacog.rs`
- `src/modules/mod.rs`
- `tools/menubar/build.sh`
- `tools/menubar/i-dream-bar.swift`

## Git Diff Summary

```
 src/modules/metacog.rs          |   37 +
 src/modules/mod.rs              |    2 +
 tools/menubar/build.sh          |   46 +-
 tools/menubar/i-dream-bar.swift | 1546 +++++++++++++++++++++++++++++++++++++--
 9 files changed, 2017 insertions(+), 298 deletions(-)
```

## Recent Commits

```
581946b   Fix four runtime issues found in pipeline audit
c76745b   Fix metacog batch truncation: use unit-boundary budget capping
1c0aaea   Raise pattern text truncation limit from 82 → 180 chars in menubar
5f091ed   Fix iw-panel content truncation via min-height: 0 on flex scroll child
f44aece   Fix payload highlighting, clickable trace chips, larger panel, 💡 widget UX
c55b70b   Fix dream-summary truncation and add lazy payload syntax highlighting
4ded1c6   Fix parse_insight_summaries to match actual blockquote format
6b4be48   Add Store Health section to menubar widget
```

## Last WAL Checkpoint

```
Done: [actions logged above]
Current: Compaction imminent (auto trigger)
Next: Continue from this checkpoint after compaction
Blockers: None
Learnings: Compaction fired (auto) — if auto, consider delegating heavy subtasks to subagents (context: fork)
===
=== CHECKPOINT [05:58] (auto compaction) ===
Goal: [see above — auto compaction fired, preserving state]
Done: [actions logged above]
Current: Compaction imminent (auto trigger)
Next: Continue from this checkpoint after compaction
Blockers: None
Learnings: Compaction fired (auto) — if auto, consider delegating heavy subtasks to subagents (context: fork)
===
=== CHECKPOINT [05:58] (auto compaction) ===
Goal: [see above — auto compaction fired, preserving state]
Done: [actions logged above]
Current: Compaction imminent (auto trigger)
Next: Continue from this checkpoint after compaction
Blockers: None
Learnings: Compaction fired (auto) — if auto, consider delegating heavy subtasks to subagents (context: fork)
===
=== CHECKPOINT [06:01] (auto compaction) ===
Goal: [see above — auto compaction fired, preserving state]
Done: [actions logged above]
Current: Compaction imminent (auto trigger)
Next: Continue from this checkpoint after compaction
Blockers: None
Learnings: Compaction fired (auto) — if auto, consider delegating heavy subtasks to subagents (context: fork)
===
```

## Recent Runtime Notes

```
# Skill Runtime Notes

## i-dream auto-insights
<!-- auto-generated by inject-dream-insights.sh on 2026-04-17 — 10 insights (conf>=0.80) -->

- **[conf=0.95]** When a session approaches 70% context usage, proactively offer to generate a structured state snapshot (like a WAL checkpoint) that includes: current task, blockers, recent decisions, file change s...
- **[conf=0.92]** Match the user's communication density. When the user sends a terse directive, respond with action first, explanation only if needed. Never ask clarifying questions that could be resolved by readin...
- **[conf=0.88]** Interpret all user messages as directives, not conversation starters. Never ask clarifying questions that could be resolved by examining project context. When a duplicate message arrives, silently ...
- **[conf=0.88]** Before running tests, deployments, or major refactors, execute a 'clean slate' checklist: kill orphaned processes, clear Playwright cache, clear frontend/build caches, and restart dev server. Proac...
- **[conf=0.88]** Treat all state as ephemeral by default. Before any context-destroying operation (session end, branch switch, worktree change, deployment), explicitly serialize and confirm preservation of current ...
- **[conf=0.88]** When the user issues a single-word or two-word directive, treat it as a job resumption signal: reconstruct intent from WAL/checkpoint state, emit a one-line acknowledgment, and begin execution. Do ...
- **[conf=0.88]** Before any change, ask: 'Did the user explicitly request this specific modification?' If the answer is no, do not make it — even if it seems like an obvious improvement. Log the temptation as a sup...
- **[conf=0.88]** At session start, explicitly confirm the task boundary. Within that boundary, execute autonomously and aggressively. Outside it, do nothing — not even 'helpful' adjacent improvements.
- **[conf=0.88]** Scale autonomy on the execution axis (more tool calls, longer runs) but never on the scope axis (no unrequested features, no 'while I'm here' improvements). High tool count ≠ permission to expand s...
- **[conf=0.88]** Hard rule: at tool call #30 in a session, auto-write a WAL checkpoint. At #60, trigger a /core-dump suggestion. These are not soft heuristics — they should be automated via a PostToolUse hook count...

---


Append-only log of post-run insights from every skill execution.
Newest entries appear at the top. Each entry is prepended by the agent after the skill completes.

## core-dump: fix-hook-3f — settings.json hooks validation error + statusline fix — 2026-04-15

**Purpose:** Document the malformed subconscious/hooks/ entries that broke settings.json validation and silently killed all settings including statusline.

**Insights:**

1. Claude Code's hooks validator skips the **entire settings file** on any schema error — a single bad hook disables statusline, permissions, env vars, model selection, and all hooks simultaneously. Always validate settings.json after any programmatic edit.
2. All four broken entries were `subconscious/hooks/` entries added with a bare `{command, type}` object at the top level instead of the required `{hooks: [{command, type}]}` wrapper — consistent with a bulk automated insertion that didn't know the schema.
3. The error message includes the 0-based array index for each bad entry, making location trivial. The format is: `EventName → N → hooks: Expected array, but received undefined`.
4. "Statusline not showing" and "hooks broken" are the same root cause when settings.json fails validation — always check the validator output first before debugging individual features.
5. Adding `/usr/bin/python3 -c "import json; json.load(open('settings.json'))"` as a sanity check after any settings edit would catch this class of error immediately.

---


  ## browser-automation-tests: JXA JS execution vs visual screenshots — brwsr-test-
  7e                                                                              
  — 2026-04-17                                                                    
```


## Additional Context Sources

- **WAL:** `.claude/wal.md` — full action log with checkpoints
- **Scratchpad:** `.claude/scratchpad/` — in-progress plans and learnings
- **Tasks:** run `TaskList` for pending agent tasks

---

_Auto-generated by pre-compact-checkpoint.sh (auto trigger)._
_Not a substitute for /core-dump (no LLM analysis of conversation history)._
